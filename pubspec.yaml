name: drawer_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.22.6
  cloud_firestore: ^0.12.10+2
  firebase_auth: ^0.15.0+1
  google_sign_in: ^4.0.1+3
  shared_preferences: ^0.5.6
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/googleicon.png
    - assets/images/nulogo.png

#EE:CD:FD:39:16:53:E6:1C:11:C8:A0:27:14:D0:9F:76:9D:E8:D1:A7
#keytool -list -v -keystore "C:\Users\Batata001\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android